---
title: "Cvičení 10: Exploratory Data Analysis - Vizualizace dat"
subtitle: "MPE_AVED - Analýza a vizualizace ekonomických dat"
author: "Dana Kozáková"
date: "`r Sys.Date()`"
output:
  html_document:
    toc: true
    toc_float: true
    toc_depth: 3
    number_sections: true
    theme: cosmo
    highlight: tango
    code_folding: show
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = TRUE,
  warning = FALSE,
  message = FALSE,
  comment = "#>",
  fig.width = 8,
  fig.height = 6
)
```

# Úvod
https://is.muni.cz/auth/el/econ/podzim2025/MPE_AVED/um/exercises/ex10.html

Cvičení na **exploratorní analýzu dat (EDA)** s důrazem na vizualizaci:

- **Vizualizovat rozdělení diskrétních proměnných** - sloupcové grafy
- **Vizualizovat rozdělení spojitých proměnných** - histogramy, hustotní grafy, box ploty
- **Analyzovat vztahy mezi proměnnými** - scatter ploty, korelace
- **Identifikovat vzory a anomálie** v datech
- **Vytvářet informativní a profesionální vizualizace**

# Načítání balíků

```{r packages}
# Načtěte potřebné balíky
library(tidyverse)

```

---

# Vizualizace rozdělení diskrétní proměnné (množství)

Diskrétní (kategorické) proměnné mají omezený počet hodnot. Pro jejich vizualizaci používáme především sloupcové grafy.

## Načtení dat

```{r diskretni_data}
# Načtěte data pro analýzu diskrétních proměnných
dfd <- readr::read_csv("https://is.muni.cz/go/t0tt37")

# Prozkoumejte strukturu dat
mean(dfd$x)

# cez lapply
dfd |> lapply(mean)

# nebo cez map v purr (purr sucastou tidyverse), je to ekvivalentne, kedysi bol map trochu rychlejší
dfd |> map(mean)

# krajsi vystup
dfd |> 
  map(mean) |>
  as.tibble()

# ale chceme len pre numericke stlpce
is.numeric(dfd)
is.numeric(dfd$x)
lapply(dfd, is.numeric)
unlist(lapply(dfd, is.numeric)) # maska, ktora vyberie len numericke stlpce

dfd[unlist(lapply(dfd, is.numeric))] |> 
  map(mean) |>
  as.tibble()

# trochu krajsi zapis predchadzajuceho 
#dfd[map_lgl(dfd, is.numeric)] |>
#  map_dbl(dfd) |> 
#  as.tibble()
```
funguje to preto, ze dataframe aj tibble sú specialne pripady listu a obe funkcie `map` aj `lapply` sú iterátory cez položky listu (= stĺpce)

**Funkce `summarise`**
- musí mať jeden výstup (pri priemere je to ok, pri kvantiloch už nemusí)
```{r}
# riesenie pomocou tidyverse, ale toto nebude fungovat, ak tam bude NA hodnota
dfd |> 
  summarise(
    across(
      where(is.numeric),
      mean
    )
  )

# Anonymní (lambda) funkcie
# 1. sposob:
dfd |>
  summarise(
    across(
      where(is.numeric), 
      ~ mean(., na.rm = TRUE) # len pre vynechanie NA hodnot
    )
  )

# 2. sposob:
dfd |>
  summarise(
    across(
      where(is.numeric), 
      \(x) mean(x, na.rm = TRUE) # \(x) je oznacenie vstupneho parametra
    )
  )

# 3. sposob: ak je funkcia zlozitejsia - na viac riadkov
dfd |>
  summarise(
    across(
      where(is.numeric), 
      \(x){ # medzi {} je telo funkcie
        na.omit(x) |>
          mean()
      }
    )
  )

# alebo vlastna funkcia, nie lambda funkcia
mean_na <- function(x) mean(x, na.rm = TRUE)

dfd |>
  summarise(
    across(
      where(is.numeric),
      mean_na
    )
  )
```
**Funkcia `reframe`** namiesto summarise
```{r}
# Využití experimentální fuknce reframe (summarise co vrací i větší počet řádků na grupu):
dfd |>
  reframe(
    across(
      where(is.numeric),
      \(x){
        # Vytvářím pojmenovaný vektor
        c(
          "Mean" = mean(x),
          "Sd" = sd(x)
        )
      }
    )
  )
```
**Riešenie so základnými funkciami**
```{r}
dfd |> 
  select(
    where(is.numeric)
  ) |>
  pivot_longer(
    everything()
    ) |>
  group_by(name) |>
  summarise(
    Mean = mean(value),
    Sd = sd(value)
  )
```
**Grafy**
```{r}
dfd |> 
  pivot_longer(everything()) |> 
  ggplot(
    aes(x = value, fill = name)
  ) +
  geom_histogram(
    alpha = 0.5 # pruhy nad sebou, default je "stack"
  ) 

dfd |> 
  pivot_longer(everything()) |> 
  ggplot(
    aes(x = value, fill = name)
  ) +
  geom_histogram(
    alpha = 0.5,
    position = "identity"
  ) 

# trochu krajsie
dfd |> 
  pivot_longer(everything()) |> 
  ggplot(aes(x = value, fill = name)) +
  geom_histogram(
    alpha = 0.5,
    position = "identity"
  ) +
  scale_fill_brewer(palette = "Set2") +
  theme_bw()

# trochu divocina so stat_bin(), takto sa to nepouziva
dfd |> 
  pivot_longer(everything()) |> 
  ggplot(aes(x = value, fill = name)) +
  stat_bin()

```
**Pouzitie jednej vlastnosti do viacerych parametrov**

- x a y sa rozlisuje aj podla farby, aj podla typu hranicnej ciary
```{r}
dfd |>
  pivot_longer(everything()) |>
  ggplot(
    aes(x = value, fill = name, linetype = name)
  ) +
  geom_density(
    alpha = 0.5
  ) +
  scale_fill_brewer(
    palette = "Set2"
  ) +
  theme_bw()
```
**Graf rug**
```{r}
dfd |>
  pivot_longer(everything()) |>
  ggplot(
    aes(x = value)
  ) +
  geom_density(
    alpha = 0.5
  ) +
  geom_rug() +
  facet_wrap(~ name) +
  theme_bw()
```
**Boxplot**
```{r}
set.seed(42)

dfd |>
  pivot_longer(everything()) |>
  ggplot(
    aes(y = value, x = name)
  ) +
  geom_jitter(
    height = 0,
    alpha = 0.2
    ) +
  geom_boxplot(
    outliers = FALSE,
    fill = NA
  ) +
  theme_bw()
```

**GGplot s gghalves**

- ale tu asi nemame kontrolu nad tym, ci sa to neprekryva
```{r}
library(gghalves)
set.seed(42)

dfd |>
  pivot_longer(everything()) |>
  ggplot(
    aes(y = value, x = name)
  ) +
  geom_half_boxplot(
    outliers = FALSE,
    fill = NA
  ) +
  geom_half_dotplot() +
  theme_bw()
```
**What's the point?**
```{r}
dfd |>
  ggplot(
    aes(x = x, y = y)
  ) +
  geom_point() +
  theme_bw()

library(ggside)

dfd |>
  ggplot(
    aes(x = x, y = y)
  ) +
  geom_point() +
  geom_xsidedensity() +
  geom_ysidedensity() +
  theme_bw()
```

## Cvičení A
V prvním příkladu zkuste z tabulky starwars (v `dplyr`) vykreslit rozdělení (četnost) barvy očí (`eye_color`). Jedná se o diskrétní proměnnou, a proto je pro vykreslení množství vhodné sáhnout pro sloupcový graf.

**Zadání:** Vytvořte sloupcový graf zobrazující četnosti kategorií.

```{r cviceni_a}
# Sloupcový graf pro diskrétní proměnnou
starwars %>%
  ggplot(aes(x = eye_color)) +
  geom_bar()

# manualne zoradenie cez faktorizovany vektor
starwars %>%
  mutate(
    eye_color = factor(eye_color, levels = c(
      "brown","blue","yellow","black","orange","red","hazel","unknown",
      "blue-gray","dark","gold","green, yellow","pink","red, blue","white"
    ))
  ) %>%
  ggplot(aes(x = eye_color)) +
  geom_bar()

# lepsie riesenie
## najprv rozdelit do grup a spocitat
starwars %>%
  group_by(eye_color) %>%
  summarise(obs = n()) # nefunguje obs = nrow(.)

## potom zoradit
starwars %>%
  group_by(eye_color) %>%
  summarise(obs = n()) %>%
  arrange(desc(obs))

## a takto zoradene priradim do levels
starwars %>%
  group_by(eye_color) %>%
  summarise(obs = n()) %>%
  arrange(desc(obs)) %>% 
  mutate(eye_color = factor(eye_color, levels = eye_color))

starwars %>%
  group_by(eye_color) %>%
  summarise(obs = n()) %>%
  arrange(desc(obs)) %>% 
  mutate(eye_color = factor(eye_color, levels = eye_color))%>% 
  ggplot(aes(x = eye_color, y = obs)) +
  geom_col() # uz nemoze byt geom_bar, lebo to nevie pracovat s dvojicou

```

**Otázky k zamyšlení:**
- Která kategorie je nejčetnější?
- Existují kategorie s velmi nízkým počtem pozorování?

---

# 📈 Vizualizace rozdělení spojité proměnné

Spojité proměnné mohou nabývat nekonečně mnoho hodnot. Pro jejich vizualizaci používáme histogramy, hustotní grafy a box ploty.

## Načtení dat

```{r spojite_data}
# Načtěte data pro analýzu spojitých proměnných


# Základní statistiky


```

## Cvičení B

**Zadání:** Vytvořte histogram pro spojitou proměnnou.

```{r cviceni_b}
# Histogram s vhodným počtem binů


```

**Otázky k zamyšlení:**
- Jak vypadá rozdělení? (normální, zešikmené, bimodální?)
- Jsou v datech extrémní hodnoty?

## Cvičení C

**Zadání:** Vytvořte hustotní graf (density plot) pro spojitou proměnnou.

```{r cviceni_c}
# Hustotní graf


```

**Srovnání:** Jaké jsou výhody hustotního grafu oproti histogramu?

## Cvičení D

**Zadání:** Vytvořte krabicový graf (box plot) pro spojitou proměnnou.

```{r cviceni_d}
# Krabicový graf


```

**Interpretace:**
- Kde je medián?
- Jaký je mezikvartilové rozpětí (IQR)?
- Existují odlehlé hodnoty?

## Cvičení E (s hvězdičkou) ⭐

**Zadání:** Vytvořte komplexní vizualizaci kombinující více typů grafů pro porovnání rozdělení spojité proměnné podle kategorií.

```{r cviceni_e}
# Kombinace histogramů/hustotních grafů podle kategorií
# Použijte faceting nebo překryvné grafy


```

**Bonusové úkoly:**
- Přidejte průměry nebo mediány pro každou kategorii
- Použijte různé barvy pro jednotlivé kategorie
- Přidejte legendu a popisky

## Cvičení F

**Zadání:** Vytvořte houslový graf (violin plot) pro porovnání rozdělení napříč kategoriemi.

```{r cviceni_f}
# Violin plot


```

**Interpretace:** Jaké informace poskytuje violin plot navíc oproti box plotu?

## Cvičení G

**Zadání:** Vytvořte kombinovaný graf s box plotem a jednotlivými body (jittered points).

```{r cviceni_g}
# Box plot s překrytými body


```

**Poznámka:** Tento typ vizualizace je užitečný pro menší datasety, kde chceme vidět jak rozdělení, tak jednotlivá pozorování.

---

# 🔍 Vizualizace vztahů v rovině

Analýza vztahů mezi dvěma proměnnými je klíčovou částí EDA. Používáme scatter ploty, korelační grafy a další.

## Načtení dat

```{r vztahy_data}
# Načtěte data pro analýzu vztahů


# Prozkoumejte strukturu


```

## Cvičení H

**Zadání:** Vytvořte bodový graf (scatter plot) zobrazující vztah mezi dvěma spojitými proměnnými.

```{r cviceni_h}
# Scatter plot


```

**Otázky k zamyšlení:**
- Existuje mezi proměnnými vztah?
- Je vztah lineární nebo nelineární?
- Jsou v datech outliers?

## Cvičení I

**Zadání:** Přidejte do scatter plotu trendovou čáru.

```{r cviceni_i}
# Scatter plot s trendovou čárou (lineární i nelineární)


```

**Úkol:** Vypočítejte korelační koeficient:

```{r cviceni_i_korelace}
# Výpočet korelace


```

## Cvičení J

**Zadání:** Vytvořte scatter plot s rozlišením podle třetí kategoriální proměnné (pomocí barvy nebo tvaru bodů).

```{r cviceni_j}
# Scatter plot s barevným rozlišením kategorií


```

**Analýza:** Liší se vztah mezi proměnnými podle kategorií?

## Cvičení K

**Zadání:** Vytvořte matici scatter plotů (pairs plot) pro exploraci vztahů mezi více proměnnými.

```{r cviceni_k}
# Matice scatter plotů
# Můžete použít GGally::ggpairs() nebo základní pairs()


```

**Interpretace:** Které páry proměnných vykazují nejsilnější vztahy?

---

# 📊 Sumarizace dat

## Popisné statistiky

```{r popisne_statistiky}
# Vypočítejte základní popisné statistiky


```

## Seskupené statistiky

```{r seskupene_statistiky}
# Vypočítejte statistiky podle skupin


```

---

# 🎨 Pokročilé techniky vizualizace

## Ridge plots (pro porovnání hustot)

```{r ridge_plot}
# Použijte ggridges balík pro ridge plot


```

## Hexbin plot (pro velké datasety)

```{r hexbin_plot}
# Hexbin pro scatter plot s mnoha body


```

## Korelační heatmapa

```{r correlation_heatmap}
# Vytvoření korelační matice a její vizualizace


```

---

# 💡 Důležité poznámky

## Výběr správného typu grafu

| Účel | Typ dat | Vhodný graf |
|------|---------|-------------|
| Rozdělení jedné kategoriální proměnné | Diskrétní | Sloupcový graf (bar plot) |
| Rozdělení jedné spojité proměnné | Spojitá | Histogram, density plot |
| Porovnání rozdělení | Spojitá + kategorie | Box plot, violin plot |
| Vztah mezi dvěma spojitými | Spojitá + spojitá | Scatter plot |
| Vztah mezi spojitou a kategoriální | Spojitá + diskrétní | Box plot, violin plot |

## Kontrola předpokladů

Při EDA kontrolujeme:
- **Normalitu rozdělení** - histogram, Q-Q plot
- **Outliers** - box plot, scatter plot
- **Linearitu vztahů** - scatter plot
- **Homoskedasticitu** - residual plots
- **Chybějící hodnoty** - `summary()`, `is.na()`

## Interpretace korelace

```r
# Korelační koeficient (Pearson)
cor(x, y)

# |r| < 0.3   → slabá korelace
# |r| 0.3-0.7 → střední korelace  
# |r| > 0.7   → silná korelace
```

**POZOR:** Korelace ≠ kauzalita!

---

# 🔧 Užitečné funkce

## Základní explorační funkce

```r
# Přehled datasetu
str(data)          # struktura
head(data)         # prvních 6 řádků
summary(data)      # popisné statistiky
glimpse(data)      # kompaktní přehled (dplyr)

# Kontrola chybějících hodnot
sum(is.na(data))
colSums(is.na(data))

# Unikátní hodnoty
unique(data$variable)
n_distinct(data$variable)
```

## Popisné statistiky

```r
# Základní
mean(x, na.rm = TRUE)
median(x, na.rm = TRUE)
sd(x, na.rm = TRUE)
var(x, na.rm = TRUE)
quantile(x, probs = c(0.25, 0.75))
IQR(x, na.rm = TRUE)

# Rozsah
range(x, na.rm = TRUE)
min(x, na.rm = TRUE)
max(x, na.rm = TRUE)
```

## Vizualizační tipy

```r
# Přidání průhlednosti pro překrývající se body
geom_point(alpha = 0.5)

# Jittering pro diskrétní data
geom_jitter(width = 0.2, height = 0)

# Faceting pro kategorie
facet_wrap(~ category)
facet_grid(rows ~ cols)

# Témata pro profesionální vzhled
theme_minimal()
theme_bw()
```

---

# 📈 EDA proces krok za krokem

## 1. Pochopení dat

- Načtení dat
- Kontrola rozměrů: `dim()`, `nrow()`, `ncol()`
- Přehled struktury: `str()`, `glimpse()`
- Názvy sloupců: `names()`, `colnames()`

## 2. Kontrola kvality dat

- Chybějící hodnoty: `summary()`, `is.na()`
- Duplicitní řádky: `duplicated()`
- Datové typy: kontrola, zda jsou správné
- Rozsah hodnot: logické minimum/maximum?

## 3. Univariátní analýza

Pro každou proměnnou:
- Popisné statistiky
- Vizualizace rozdělení
- Identifikace outliers
- Kontrola normalitu (pokud relevantní)

## 4. Bivariátní analýza

- Scatter ploty pro spojité proměnné
- Box ploty pro spojitou vs. kategoriální
- Korelační analýza
- Testy vztahů

## 5. Multivariátní analýza

- Matice scatter plotů
- Korelační heatmapy
- Facetované grafy
- Interakce mezi proměnnými

---

# 🎯 Checklist

- [ ] Rozumím rozdílu mezi diskrétními a spojitými proměnnými?
- [ ] Vím, jak vizualizovat rozdělení jedné proměnné?
- [ ] Dokážu porovnat rozdělení napříč kategoriemi?
- [ ] Umím analyzovat vztahy mezi dvěma proměnnými?
- [ ] Vím, jak identifikovat outliers a anomálie?
- [ ] Dokážu interpretovat korelační koeficienty?
- [ ] Umím vytvořit informativní a čitelné grafy?
- [ ] Chápu limitace korelační analýzy?

---

# 🔗 Užitečné odkazy

- [R for Data Science - EDA](https://r4ds.had.co.nz/exploratory-data-analysis.html)
- [ggplot2 documentation](https://ggplot2.tidyverse.org/)
- [GGally package](https://ggobi.github.io/ggally/) - pro pairs plots
- [ggridges package](https://wilkelab.org/ggridges/) - pro ridge plots
- [R Graph Gallery - Distribution](https://r-graph-gallery.com/distribution.html)
- [Correlation methods](https://www.statmethods.net/stats/correlations.html)

---

# 📚 Doporučená literatura

- **Wickham & Grolemund**: R for Data Science (kapitola 7: EDA)
- **Tukey, J. W.**: Exploratory Data Analysis (1977) - klasická práce
- **Healy, K.**: Data Visualization: A Practical Introduction

---

<div style="text-align: center; margin-top: 50px; padding: 20px; background-color: #f0f0f0; border-radius: 10px;">
**Vytvorené pre:** MPE_AVED, Týždeň 10, ECON MUNI  
**Dátum:** `r Sys.Date()`
</div>
